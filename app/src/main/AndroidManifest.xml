<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vserp.moneyflow">

    <application
        android:allowBackup="true"
        android:icon="@drawable/moneyflow_icon_64"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dialogs.AddNewExpenseDialog" />

        <service
            android:name=".services.MyIntentService"
            android:exported="false" />

        <provider
            android:name=".provider.MyContentProvider"
            android:authorities="com.example.vserp.moneyflow.provider"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activities.ExpensesActivity"></activity>
    </application>

</manifest>